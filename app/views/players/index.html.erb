<h1>Players List</h1>

<p>I'm the coder, i code</p>


<ul>
  <% @players.each do |player| %>
    <li><%= link_to player.name, player_path(player)%></li>
  <% end %>
</ul>

  <%= link_to "Add a new Player", new_player_path, class: 'btn btn-primary' %>


<p>Now is <%= Time.now %></p>
